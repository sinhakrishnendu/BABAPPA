#!/usr/bin/env bash
set -euo pipefail
echo 'MANUAL EXECUTION SCRIPT - REVIEW BEFORE RUNNING'

# (cd codeml/WRKY_candidate_02_close && codeml branch_site_model_A.ctl && codeml branch_site_null.ctl)
# After runs: compute LRT statistics and BH correction into reference_results.tsv.
